# Smartwatch_LLM_Service
Smartwatch LLM Service is a backend service that uses multimodal language models (LLMs) to analyze smartwatch images and extract health metrics (heart rate, steps, calories, etc.).
